## 시작 -> 실행 -> ISE
1. login-azaccount

2. 리소스 그룹명, 가상머신 이름
$vm = Get-AzVM -ResourceGroupName ISCREAM -VMName TestVM

3. 변경할 사이즈 이름
$vm.HardwareProfile.VmSize = "Standard_DS3_v2"

4. 사이즈 변경
Update-AzVM -VM $vm -ResourceGroupName ISCREAM
